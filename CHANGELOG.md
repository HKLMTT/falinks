# Changelog

## 0.10.0

- **控制台全屏渲染，复刻 Claude Code 的滚动体验**：进入 alternate screen 自绘视口，**滚轮直接上滚回看历史（无需任何快捷键）、底部输入区钉死不随滚动**，拖选复制保持终端原生（不开鼠标上报）。原理与 Claude Code 相同：alternate scroll(1007) 让终端把滚轮转成方向键。回看中 ↑↓ 逐行滚、`PageUp/PageDown` 翻页、`Esc` 或直接打字跳回最新；全帧用 synchronized output(2026) 包裹，不闪烁；退出后终端还原到启动前内容。
- **ask 选择题在回答时支持滚轮看上下文**：答题浮层开着时滚轮代行 ↑↓ 移动选项（同 Claude Code 手感）。
- **"等送达"显示排队条数**：同一收件人多条排队不再只显示一个目标，按目标聚合成 `→ dev ×3 · → qa`。
- **排队消息可取消**：输入框为空时按 `Esc` 弹出排队清单（↑↓ 选 · Enter 取消该条 · Esc 关闭），可连续取消多条；被取消的消息永不投递，历史里该条标红色 `✗已取消`（记录保留不消失）。新增 `POST /admin/cancel` 端点。
- **修复：恢复历史后消息漏显示**——重启恢复持久化历史后，新消息 id 与旧消息撞号（都从 m0 起算），控制台同 key 漏渲染。现在新消息序号从历史最大号之后继续，且同批撞号去重。
- **修复：按键偶发丢失/打进过期状态**——键盘监听器原先每帧重挂，按键若落在状态更新与重挂之间会被旧逻辑处理（如 Esc 打开浮层后紧跟的 Enter 丢失）。改为监听器只挂一次、永远调用最新渲染的处理闭包。

## 0.9.0

- **组长(协调者)工作法**:可把某员工设为组长,启动时给它注入"先和 boss 对齐需求 → 用 superpowers(brainstorming + writing-plans)完整设计(设计阶段可调度他人协助、但不编码)→ 方案敲定后才拆解、分派、管理任务"的工作法。预设"全栈小组"的 lead 自动带;config 里给某 agent 加 `"lead": true` 即生效。
- **`/lead` 命令**:控制台输入 `/lead` 弹选择器选一个员工当组长(全队唯一,换 lead 自动取消旧的);当场注入工作法、旧 lead 收到卸任通知、写回 config、`/clear` 后仍保留;花名册该员工显示 `♔组长`。
- **自定义建团队增加"选工作目录"**:setup 自定义流程从 `名字 → CLI → 角色` 增加到 `… → 工作目录`(带目录补全,↑↓ 选 · Tab 补全 · Enter 确认),每个成员可独立工作目录。
- **全栈小组预设新增 UI/UX 角色**:`组长 + 前端 + 后端 + 测试 + UI/UX`(UI/UX 负责设计走查、把控统一风格)。
- **修复:粘贴图片后消息发不出去**——图片占位 `[图片N]` 展开成 `/var/...png` 后若以 `/` 开头会被误判成命令。现在用原始输入判命令、只对消息体展开路径。
- **鼠标滚轮默认关**:默认即可原生拖选复制(不必按 Option);想用滚轮翻历史再 `/mouse` 开。(注:`PageUp/PageDown` 翻历史始终可用。)

## 0.8.0

- **修复:工作中的员工被误判为"空闲"**(老问题)。状态检测不再靠截屏正则(新版 Claude 底部常驻 `bypass permissions on (shift+tab to cycle)` 状态行会让旧逻辑永远判不忙→投递后约 4.5s 必被误降空闲),改用 iTerm2 原生信号 **`is processing`**(最近~2s 有输出;干活的 CLI 持续刷 spinner=持续有输出),截屏作廉价兜底。实测:持续工作期间稳定判忙、不再闪烁;真正停手后约 5s 才降闲。`FALINKS_DEBUG_BUSY=1` 可开逐轮询诊断日志排查。
- **pane 角色视觉身份**:每个员工 pane 按角色染**背景色**(与控制台花名册同一套配色、同色相对应:花名册里 lead 绿 ↔ lead 的 pane 深绿底)+ 右上角**员工名徽章**水印,一眼认出哪个窗口是谁(不再被 CLI 自改的标题盖掉)。`config.paneTheme: false` 可整体关闭(尊重自定义配色用户)。
- **输入目标更醒目**:输入提示符前新增彩色目标 chip,随输入实时变——纯文本 `→ 名字`(该成员角色色)、`@all` 显 `📢 全员`(黄)、`@某人` 显对应成员、`/` 显 `⌘`、无上次目标显提示。一眼知道这条会发给谁。

## 0.7.0

- **回看滚动重构(符合滚动习惯 + 不再遮挡)**：往上滚时光标(❯)先在可见消息间逐条移动、**视口不动**,撞到顶部那条后再往上才翻页带进更早的消息(以前是每滚一下整屏平移一条)。选中条恒在视口内**完整可见**——把中文/全角按 2 列**折行**算进了行高估算,窗口贴合可用高度,不再因多行长消息溢出而把选中条正文或最新几条裁到屏幕外。
- **协作死锁可观测性**：反复出现"全员空闲、任务却没完成"时现在能定位。三类"会悄悄打断协作"的事件落盘到 `~/.falinks/diag/<项目hash>.jsonl`——`guard-drop`(消息被守卫按回合上限/循环/限流丢弃)、`inject-fail`(消息已出队却注入失败=永久丢失)、`auto-idle`(投递后异常快被自动判空闲,可能没回完就被下条排队消息叠注入打断)。控制台消息区在有这些事件时顶一行黄色告警;新增 `/admin/diag` 端点。
- **修复:`sendmsg`/`ask` 把"被守卫拦下"误报成"unknown or dead target"**——agent 会误以为对方已下线而放弃、导致流程中断。现在准确区分"目标不存在 / 目标已死 / 被守卫拦截(明示未送达)"。
- **`/clear` 全员时一并清空 boss 历史对话**：不带名的 `/clear`(清全员)现在连消息区的 boss 历史流水(内存 + 持久化)和诊断流水也清空;指定 `/clear <名字>` 只清那一个 pane 的上下文,boss 历史保留。

## 0.6.0

- **消息按 Markdown 渲染**：agent 消息里的 `**粗体**`、`*斜体*`、`` `行内代码` ``、~~删除线~~、标题、有序/无序列表、引用、分割线现在直接渲染成样式（仿 Claude Code：marked 解析 → 样式片段 → Ink 组件，不塞 ANSI、排版交给 Ink），围栏代码块逐行变暗原样显示。不再满屏 `**`/`` ` `` 噪音。
- **鼠标滚轮回看历史**：消息区支持鼠标滚轮上下滚动翻看历史（默认开启，等价 `PageUp`/`PageDown`，到底自动恢复跟随最新）。`/mouse` 命令可随时开关——关闭后恢复终端原生的拖选复制（开启时按住 Option 拖仍可选）。退出时自动还原终端鼠标状态。
- **消息区高度自适应**：可见消息条数随终端窗口高度自动增减，不再固定只显示 6 条、下方留一大片空白。
- **修复：员工状态永久卡在"忙/转圈"降不回空闲**（`/clear` 后、尤其多员工并发或边生成边清时易触发）：读屏判忙改为只看底部活区，不再被 scrollback 里残留的旧进度行（spinner 帧）误判，已空闲的员工能正常回到 `空闲`。

## 0.5.0

- **boss 端历史回看**：消息区支持 `PageUp` / `PageDown` 在消息条目间选择（避开被 macOS 系统热键占用的 Ctrl+↑↓），`Enter` 就地展开/收起选中消息的完整正文（不必再切到员工窗口看），`Esc` 或直接打字退出回看。消息流水保留上限从 300 提到 **5000**（可在配置里用 `historyCap` 调整），真正能往回翻看更早的历史。
- **消息投递状态徽标**：消息行 `→ 目标` 后显示「⏳ 排队中 / ✓ 已送达」——投给正在忙的员工会排队等待、目标空闲后自动转「已送达」。判据是消息此刻是否仍在目标 inbox 里（实时、重启后历史不误判）；只对投给真实员工的消息显示。
- **花名册状态本地化**：状态标签随语言显示「启动中 / 空闲 / 工作中 / 卡住 / 已下线」（英文环境保留英文），不再固定英文。
- **修复：员工在干活、花名册却显示 idle**：`isPaneBusy` 原先只认状态行最右段「esc to interrupt」，窄分屏里这段被 pane 宽度裁掉、读屏抓不到，导致正在生成的员工被判空闲。现在改认裁不掉的生成 spinner 计时器，且与用户可自定义的 statusLine 完全无关。

## 0.4.3

- 文档:README 改为双语——README.md 英文版(npm/GitHub 首页),新增 README.zh-CN.md 中文版,顶部互相链接。

## 0.4.2

- 修复:控制台输入 `/lang`(及 `/help`)后回车不再补出尾空格、像在等参数;无参命令补全时回车/Tab 直接执行——`/lang` 回车即弹语言选择器。

## 0.4.1

- 无功能变更:验证 GitHub Actions + npm Trusted Publishing(OIDC)自动发布链路;发布产物带 provenance 溯源签名。

## 0.4.0

- **多个项目可同时各开一间办公室**：端口自动分配（`busPort` 不再必填，缺省由系统分配；显式指定的端口被占时自动回退并在控制台提示）；每个项目独立维护一份 `~/.falinks/runtime/<hash>.json` 档案，互不干扰；`falinks say/roster/log` 等子命令按当前目录寻址——活着的实例只有一个时任意目录可用，多个时列出各项目路径让你选；同目录防双开（已有活实例则报错退出）；崩溃残留档案自动探活自愈（用 pid + busPort 做身份核对，防止端口复用劫持）。新生成的配置/模板不再写 `busPort` 字段。
- **中英双语界面（i18n）**：UI 文案、CLI 提示、启动向导、员工协作规则、slogan 全量双语。默认跟随系统语言（`LANG`/`LC_ALL` 等环境变量，`zh` 开头=中文，否则英文）。控制台 `/lang` 弹选择器即时切换，`falinks lang` 在启动前设置；设置写入 `~/.falinks/settings.json`，重启后保留。
- **修复：员工在干活、花名册却显示 idle**：状态原先只靠消息记账、且健康轮询只做单向降级，长任务里「esc to interrupt」短暂离开视口就被误判空闲且回不来。现在按 pane 实况双向校准（在生成→升 busy，连续多次确认空闲才降 idle），状态如实反映。
- **控制台不再闪烁，消息贴底显示**：界面输出严格裁剪进视口（不再超出终端高度触发整屏重绘），轮询数据没变化时不重渲染——空闲和干活时画面都稳定不闪。消息区改为贴底滚动（最新消息始终可见，旧的自动让位），输入框固定在底部。在忙员工的状态点会转动画。
- **修复：窗口 resize 后 logo 重复/换行错乱**：resize 时整屏清一次并按新宽度重绘，不再残留旧画面。
- **修复：`/add` 失败后彻底无法再添加员工**：分屏锚点野指针（`splitFrom` 指向已关闭 pane）导致后续所有 `/add` 卡死，现在自愈清除野指针，加员工可正常重试。

## 0.2.6

- **真 Shift+Enter 换行(开箱即用)**：控制台改为自研键盘输入层——启动时开启 kitty 键盘协议、自己解码按键,于是 Shift+Enter 能与回车区分、直接换行,**无需任何终端配置**(不支持 kitty 的终端自动降级为 `\` + 回车)。中文输入、方向键、历史、补全、Ctrl 组合均照常。
- **启动快多了**：员工的就绪检测/注入/抓取改为后台进行,建好 pane 控制台**约 1-2 秒即渲染**,不再干等数十秒;启动期间是干净的「正在准备…」画面,不再刷裸日志;员工就绪后花名册自动 launching→idle。
- **花名册/消息按人配色且不重复**：调色板扩到 20 色,按花名册顺序给每人分配**唯一**颜色(消息面板复用同色),一眼区分谁是谁。
- **`[图片N]` 整体删除**：退格遇到图片占位时整块删掉(像 Claude Code),不再逐字符删。
- **`/clear` 并发 + 清空期间消息排队**：多员工同时清(总耗时≈单个);清空期间该员工标"忙",发来的消息**排队**、清完自动投出,不会被冲掉或串进清空过程。
- **修复:员工偶发误下线**：pane 探测在 osascript 高并发下会瞬时误报,曾导致员工从花名册消失但窗口还在(且退出关窗会残留它);改为**连续 3 次探不到才下线**。
- **退出关窗更稳**：选择关闭员工窗口时并行关 + 兜底再查一遍重关漏网的,修复偶尔遗留一个 pane 没关。
- README 更新到当前功能。

## 0.2.5

- **修复:派活消息卡死不投递**。以前消息发给"忙碌"员工只排队,要等员工主动调 `idle` 工具才投出;员工生成完不调 idle、或被人 `Ctrl+C` 打断时,路由永远停在 busy,排队的消息(如 lead 给其他员工派的活)永不送达(左侧 log 有记录但 pane 收不到)。现在 1.5s 轮询读屏自动识别员工已空闲(屏上没有生成中的「esc to interrupt」、且距上次投递已过宽限),自动投出排队消息,约 1.5 秒内送达,不再依赖员工自报 idle。
- **消息面板更易读**:从"一行压扁截断"改成分块多行——保留换行、按宽度折行、条间留白;最新一条显示全文,历史每条 3 行预览 +「… +N 行」。
- **选项模式(ask 工具)**:员工可用 `ask(to, question, options)` 出选择题。发给老板时控制台渲染成箭头 picker(↑↓ 选 · Enter 回复),答案注回提问员工;发给同事则对方收到带编号选项的消息。
- **防误群发**:纯文本不再群发——`@all` 才群发;纯文本=回复"上一次对话目标"(最近一条与你对话的对方),`@` 补全含 `all`。
- **输入框 Home/End**:支持物理 Home/End(及 Ctrl+A/Ctrl+E)跳行首/行尾。
- **修复:消息被护栏静默丢弃**。线程归属改为「按对方判定」——只有回复你正在处理的那条消息的发信人才续用同一线程,发给别人=新线程。于是:① 协调者(lead)给多人派活各是独立线程,不互相挤占回合数;② boss(虚拟)发起的长对话每条都是新线程,**不会**因 `maxTurnsPerThread` 累计而被丢(修复"聊久了 boss→员工 回车没发出去");③ agent 之间真·来回循环仍共用线程、照样被回合/循环护栏拦。控制台 `say`/回复 现在如实显示「未送达」而非假成功。
- **修复:恢复会话不再重做旧任务**。以前恢复时会注入"请重新 register"提示、且 CLI `--resume` 重放历史,员工会把第一次的任务又做一遍;现在由 falinks 服务端直接登记(它本就知道 sessionId),恢复时**什么都不注入**——员工 CLI 静默恢复、挂回总线、待命,不重做。
- **消息历史持久化**:消息流水存到 `~/.falinks/messages/<项目>.jsonl`(滚动上限默认 300 条,超量自动压缩),重启/继续团队后历史对话仍在,不再清空。
- **消息面板配色/时间**:每个发言人按名字稳定配色;行首显示对话时间 `HH:MM:SS`;箭头改为 ` → `(前后留空格)。
- **`/clear` 清空上下文**:控制台 `/clear <名字>` 清某员工、`/clear` 不带名清全员。清空后**自动重注入 bootstrap**(身份+协作规则),员工不会失忆、会重新报到待命。
- **Ctrl+C 退出先确认**:在控制台按 Ctrl+C 不再直接退,先问「关闭所有员工窗口吗？」——`y/Enter`=关闭员工 pane 并退出、`n`=保留窗口退出、`Esc`=取消。
- **多行输入**:输入框支持多行,`\` + 回车 = 换行(零配置、所有终端开箱即用),普通回车仍=发送。(真正的 Shift+Enter 需要接管整个键盘输入层,后续版本再做。)

## 0.2.4

- **启动即查更新**：更新检查前移到最开始、选团队之前。发现新版先弹一屏让你选「继续使用当前版本 / 退出去更新」（选退出会打印 `sudo npm i -g @liujia307/falinks` 后退出）；不再像以前那样办公室全起好后才在控制台被动显示一行。离线/查不到则跳过、不打扰。

## 0.2.3

- **会话恢复**：同目录再次启动并选「继续当前团队」时，每个员工带着上次对话记忆恢复——claude 启动即用 `--session-id` 钉死会话 id、恢复用 `--resume`；codex 就绪后注入 `/status` 读屏抓 id（轮询重试，员工忙时也能抓到）、恢复用 `codex resume <id>`。会话档按项目目录存于 `~/.falinks/sessions/`；换团队=全新会话；恢复只注入一句重连提示、不重发 bootstrap（省 token）。
- **团队名单可恢复**：控制台 `/add`、`/remove` 现在写回 `falinks.config.json`——动态加的员工（含 codex）关窗后选「继续当前团队」能完整拉回，不再消失。
- **自定义团队可选 codex**：启动时的自定义团队构建器加了一步选 CLI（claude / codex），不再写死 claude。

## 0.2.2

- **每次启动都能选团队**：不再"有配置就跳过"。已有配置时选单顶部默认「继续当前团队」（回车即沿用），上下选可换团队——每个项目用不同团队不必删配置文件。
- **更新提示带命令**：检测到新版只**提示**（不自动更新），显示新版本号 + `sudo npm i -g @liujia307/falinks`。
- **控制台粘贴截图**：`Ctrl+V` 读系统剪贴板里的图存临时 PNG，输入框显示短占位 `[图片N]`，`@员工 + 回车`后展开成真实路径，员工自己去读图。
- **消息不再截断**：消息流水显示完整内容（合并空白、上限 300 字），不再只显示首行 60 字。
- **输入框光标 + 历史**：← → 移动光标、在任意位置增删；无补全时 ↑↓ 翻历史输入。
- **`@` 补全排除 boss**：你自己就是 boss，不再提示 @自己。

## 0.2.0

- **单窗口办公室**：在当前 iTerm 窗口里直接渲染控制台 + 向右 split 员工，不再多弹一个窗口（非 iTerm/后台回退为新开窗口）。
- **团队模板**：每次启动 Ink 选单选团队（单人 / 结对 / 全栈 / 调研），已有配置时默认「继续当前团队」回车即沿用；也可现场自定义并存成自己的模板（`~/.falinks/templates/`）。
- **`/add` 向导加「角色」步骤**：名字 → cli → 角色/职责 → 工作目录。
- **启动检测新版本**：控制台静默查 npm，有新版在 logo 下**提示**版本与更新命令（`sudo npm i -g …`，不自动更新）。
- **控制台门面**：ASCII logo + 版本号 + 随机牛马文案；渲染前清屏，盖掉选单/启动日志。
- **pane 标题 = 员工名**（持续覆盖 CLI 自改的标题）。
- **中文/非 ASCII 员工名修复**：总线对 URL 路径解码，中文名也能注册并回消息。
- 端口被占时友好提示（不再崩栈）；`init` 默认员工目录=当前目录、默认单员工。

## 0.1.x

- 首个发布：iTerm2 多 agent 编排，MCP 总线（register/sendmsg/idle/who），claude/codex 员工，控制台（@/ 补全、say/broadcast/roster/log），防失控护栏，关窗自动下线，`falinks init/doctor/up`、一键 `falinks`。
