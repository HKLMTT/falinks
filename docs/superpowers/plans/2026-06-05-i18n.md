# i18n 中英双语实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** falinks 全量文案中英双语:默认跟随系统语言,`/lang`(控制台)与 `falinks lang`(CLI)弹选择器切换,全局持久化,运行中切换自然生效。

**Architecture:** 自研轻量 i18n——`src/i18n/zh.ts` 中文基准词典(字符串+插值函数),`en.ts` 用 `typeof zh` 类型钉死对齐(漏译=编译失败),`t()` 按模块级 locale 取词。设置存 `~/.falinks/settings.json`。控制台与 up 可能跨进程,经 `POST /admin/lang` 传播。模块初始 locale 恒为 'zh',只有进程入口调 `initLocale()`(测试确定性)。

**Tech Stack:** TypeScript + React/Ink + vitest。无新依赖。

**规格:** `docs/superpowers/specs/2026-06-05-i18n-design.md`

**总原则:** 词典 key 用语义命名(`clipboardNoImage`,不是 `msg42`);提取文案时**逐字保留**现有中文(它是基准);英文翻译口吻与中文对应(slogan 重写成英文梗)。每个 Task 结束 `npx tsc --noEmit && npm test` 全绿再 commit(基线 213,只增不减)。

---

### Task 1: settings.ts + i18n 核心模块

**Files:**
- Create: `src/settings.ts`、`src/i18n/index.ts`、`src/i18n/zh.ts`(本任务只放 2-3 条种子词条)、`src/i18n/en.ts`
- Test: `tests/settings.test.ts`、`tests/i18n.test.ts`

- [ ] **Step 1: 失败测试** `tests/settings.test.ts`:

```ts
import { mkdtempSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { expect, test } from 'vitest';
import { loadSettings, saveSettings } from '../src/settings.js';

const tmpRoot = () => mkdtempSync(join(tmpdir(), 'falinks-set-'));

test('缺省 settings:locale=auto', () => {
  expect(loadSettings(tmpRoot())).toEqual({ locale: 'auto' });
});

test('保存后读回', () => {
  const root = tmpRoot();
  saveSettings({ locale: 'en' }, root);
  expect(loadSettings(root)).toEqual({ locale: 'en' });
});

test('损坏文件回退默认', () => {
  const root = tmpRoot();
  saveSettings({ locale: 'zh' }, root);
  require('node:fs').writeFileSync(join(root, 'settings.json'), '{broken');
  expect(loadSettings(root)).toEqual({ locale: 'auto' });
});

test('非法 locale 值回退 auto', () => {
  const root = tmpRoot();
  require('node:fs').writeFileSync(join(root, 'settings.json'), JSON.stringify({ locale: 'fr' }));
  expect(loadSettings(root)).toEqual({ locale: 'auto' });
});
```
(ESM:`require` 改顶部 `import { writeFileSync } from 'node:fs'`。)

`tests/i18n.test.ts`:

```ts
import { expect, test, afterEach } from 'vitest';
import { t, getLocale, setLocale, detectLocale } from '../src/i18n/index.js';
import { zh } from '../src/i18n/zh.js';
import { en } from '../src/i18n/en.js';

afterEach(() => setLocale('zh')); // 测试间复位基准语言

test('初始 locale 是 zh(基准,保证全套测试确定性)', () => {
  expect(getLocale()).toBe('zh');
});

test('setLocale 切换 t() 的词典', () => {
  expect(t()).toBe(zh);
  setLocale('en');
  expect(t()).toBe(en);
  expect(getLocale()).toBe('en');
});

test('detectLocale:LC_ALL > LC_MESSAGES > LANG,zh 开头=zh,否则 en,全空回退 zh', () => {
  expect(detectLocale({ LANG: 'zh_CN.UTF-8' })).toBe('zh');
  expect(detectLocale({ LANG: 'en_US.UTF-8' })).toBe('en');
  expect(detectLocale({ LC_ALL: 'zh_TW.UTF-8', LANG: 'en_US.UTF-8' })).toBe('zh');
  expect(detectLocale({ LC_MESSAGES: 'ja_JP.UTF-8', LANG: 'zh_CN.UTF-8' })).toBe('en');
  expect(detectLocale({})).toBe('zh');
  expect(detectLocale({ LC_ALL: '', LANG: 'zh_CN.UTF-8' })).toBe('zh'); // 空串跳过
});

test('en 与 zh 的 key 集合完全一致(防 as any 逃逸)', () => {
  expect(Object.keys(en).sort()).toEqual(Object.keys(zh).sort());
});
```

- [ ] **Step 2: 跑红** `npx vitest run tests/settings.test.ts tests/i18n.test.ts` → FAIL(模块不存在)。

- [ ] **Step 3: 实现**

`src/settings.ts`:

```ts
import { readFileSync, writeFileSync, mkdirSync } from 'node:fs';
import { join } from 'node:path';
import { runtimeDir } from './runtime.js';

/** 用户全局设置:~/.falinks/settings.json。损坏/缺失/非法值一律回退默认。 */
export interface Settings { locale: 'zh' | 'en' | 'auto'; }
const DEFAULTS: Settings = { locale: 'auto' };

export function settingsPath(root = runtimeDir()): string {
  return join(root, 'settings.json');
}

export function loadSettings(root = runtimeDir()): Settings {
  try {
    const d = JSON.parse(readFileSync(settingsPath(root), 'utf8'));
    const locale = d?.locale === 'zh' || d?.locale === 'en' || d?.locale === 'auto' ? d.locale : DEFAULTS.locale;
    return { locale };
  } catch { return { ...DEFAULTS }; }
}

export function saveSettings(s: Settings, root = runtimeDir()): void {
  mkdirSync(root, { recursive: true });
  writeFileSync(settingsPath(root), JSON.stringify(s, null, 2));
}
```

`src/i18n/zh.ts`(种子词条,Task 2 起逐步搬入全部文案):

```ts
/** 中文基准词典:全部用户可见/注入员工的文案都从这里出。纯文本=字符串,带变量=箭头函数。 */
export const zh = {
  clipboardNoImage: '剪贴板里没有图片',
  officeReady: (n: number) => `✅ 办公室就绪：${n} 名员工 + 控制台。Ctrl-C 收工。`,
};
```

`src/i18n/en.ts`:

```ts
import type { zh } from './zh.js';

/** 英文词典:typeof zh 钉死 key 与签名——漏译/签名不符直接编译失败。 */
export const en: typeof zh = {
  clipboardNoImage: 'No image in clipboard',
  officeReady: (n: number) => `✅ Office ready: ${n} workers + console. Ctrl-C to wrap up.`,
};
```

`src/i18n/index.ts`:

```ts
import { zh } from './zh.js';
import { en } from './en.js';
import { loadSettings } from '../settings.js';

export type Locale = 'zh' | 'en';

// 初始恒为 'zh'(基准语言):库代码与测试不读用户 settings、不受 CI 的 LANG 影响。
// 只有进程入口(cli.ts / console/main.tsx)调用 initLocale() 接入用户设置与系统语言。
let current: Locale = 'zh';

export function getLocale(): Locale { return current; }
export function setLocale(l: Locale): void { current = l; }

/** 按当前语言取词典。用法:t().officeReady(3) */
export function t(): typeof zh { return current === 'zh' ? zh : en; }

/** 系统语言判定:LC_ALL > LC_MESSAGES > LANG 取首个非空,zh 开头=中文,否则英文;全空回退中文。 */
export function detectLocale(env: Record<string, string | undefined>): Locale {
  const v = [env.LC_ALL, env.LC_MESSAGES, env.LANG].find((x) => x && x.length > 0);
  if (!v) return 'zh';
  return v.toLowerCase().startsWith('zh') ? 'zh' : 'en';
}

/** 进程入口初始化:settings 的 locale,'auto' 时按系统语言判定。 */
export function initLocale(): Locale {
  const s = loadSettings();
  const l = s.locale === 'auto' ? detectLocale(process.env) : s.locale;
  setLocale(l);
  return l;
}
```

- [ ] **Step 4: 跑绿** 上述两个测试文件全 PASS;`npx tsc --noEmit` + `npm test` 全绿。

- [ ] **Step 5: Commit** `git add src/settings.ts src/i18n tests/settings.test.ts tests/i18n.test.ts && git commit -m "feat(i18n): 核心模块(zh 基准/en 类型钉死/detectLocale)+ settings.json 持久化"`

---

### Task 2: 控制台 UI 文案全量迁移(console/*)

**Files:**
- Modify: `src/i18n/zh.ts`、`src/i18n/en.ts`(扩词典)
- Modify: `src/console/app.tsx`、`src/console/commands.ts`、`src/console/parse.ts`、`src/console/clipboard.ts`
- Test: `tests/console/clipboard.test.ts`(扩)、现有 console 测试(改引用)

**做法:** 通读 4 个文件,把每条中文文案**逐字**搬进 `zh.ts`(语义化 key,按文件分组注释),源码改 `t().key` / `t().key(args)`;`en.ts` 同步补英文。TAGLINES → `zh.taglines: string[]`(9 条原文)/ `en.taglines`(英文梗重写,如 "A room of AI workhorses — you just talk 🐴" 风格,9 条)。

- [ ] **Step 1(先红): [图片N] 双语识别测试** `tests/console/clipboard.test.ts` 追加:

```ts
test('英文占位符 [Image N] 同样展开(切语言后旧占位符也不失效)', () => {
  const paths = ['/tmp/a.png'];
  expect(expandImageTokens('@bob see [Image 1]', paths)).toBe('@bob see /tmp/a.png');
  expect(expandImageTokens('混用 [图片1] and [Image 1]', paths)).toBe('混用 /tmp/a.png and /tmp/a.png');
});
```
跑红:`npx vitest run tests/console/clipboard.test.ts`。

- [ ] **Step 2: clipboard.ts 双语化**
- `expandImageTokens` 的正则改为 `/\[(?:图片|Image )?(?:图片|Image\s?)(\d+)\]/` —— 注意:**以现有实现的正则为基准**改成 `\[(?:图片|Image\s?)(\d+)\]` 形式(先读现有代码再改,保持其余逻辑不动)。
- 生成端:`[图片${n}]` 改为 `t().imageToken(n)`,词典:zh `(n) => \`[图片${n}]\``,en `(n) => \`[Image ${n}]\``。
- `app.tsx` 退格整删正则 `\[图片\d+\]$` 改为 `\[(?:图片|Image\s?)\d+\]$`。
跑绿。

- [ ] **Step 3: app.tsx 全量文案 → 词典**
涉及(对照现有代码逐条,不漏):TAGLINES、/help status、`⚠ 未送达`、`被护栏拦截`、`→ 全员`、`没有上次对话目标…`、`＋/－/🧹 已清空`、`已取消添加`、`📎 已附加…`、`✓ 已回复…`、退出确认两行、向导三步全部提示与示例、答题区文案(`❓ X 问你`、`↑↓ 选 · Enter 回复…`、`还有 N 个待答`)、底部提示行、`群发全员` hint、花名册/消息标题(`花名册`/`消息`)、`… +N 行(完整见 X 窗口)`。
`commands.ts`:COMMANDS 的 usage/hint 改为 getter 或函数化——**注意**:COMMANDS 是模块级常量,文案要随 locale 动态变,改成 `export function commands(): CommandDef[]`(内部 `t()`)或 hint 字段存 key;**选最小改动**:COMMANDS 保留 name,`usage`/`hint` 改从 `t().cmdUsage[name]`/`t().cmdHint[name]` 取(词典里建 `cmdUsage`/`cmdHint` 对象),`commandState` 不变。先看调用方(app.tsx、补全测试)再定,保持现有测试通过(测试若断言中文 usage,默认 zh 不受影响)。
`parse.ts`:错误信息(`用法: /remove <name>` 等)→ `t()`。

- [ ] **Step 4: 跑绿 + 提交**
`npx tsc --noEmit && npm test` 全绿(默认 zh,现有中文断言不破)。
`git add -A && git commit -m "feat(i18n): 控制台 UI 全量文案入词典(含 [图片N]/[Image N] 双语识别与 taglines 英文版)"`

---

### Task 3: CLI / setup / update 文案迁移

**Files:**
- Modify: `src/i18n/{zh,en}.ts`、`src/cli.ts`、`src/setup/app.tsx`、`src/setup/run.tsx`、`src/update.ts`
- 入口接线:`src/cli.ts` main() 开头 + `src/console/main.tsx` 顶部调 `initLocale()`

- [ ] **Step 1:** 通读 4 文件,全部中文文案入词典(分组注释),源码改 `t()`:cli 帮助/doctor 各项与提示/`✅ 配置已就绪`/`没找到配置…`/默认 bootstrap 文本(`你是办公室里的 AI 员工,风格简练。`→ 词典 `defaultBootstrap`)/`已退出。更新命令:`;setup 的更新弹屏/团队选单/自定义向导/`·我的`/`＋ 自定义团队…`/`保存为团队模板…`;update.ts 如有文案一并。
- [ ] **Step 2: 入口接线** `cli.ts` 的 `main()` 第一行 `initLocale();`;`console/main.tsx` 顶部(解析 --port 之前)`initLocale();`。
- [ ] **Step 3:** `npx tsc --noEmit && npm test` 全绿。
- [ ] **Step 4:** `git add -A && git commit -m "feat(i18n): CLI/setup/update 文案入词典 + 进程入口 initLocale 接线"`

---

### Task 4: 注入员工的文案迁移(index/orchestrator/bus/templates)

**Files:**
- Modify: `src/i18n/{zh,en}.ts`、`src/index.ts`、`src/orchestrator.ts`、`src/bus/server.ts`、`src/templates.ts`、`src/core/guards.ts`

- [ ] **Step 1(先红):** 现有 orchestrator 测试断言 `【来自`——保持默认 zh 应仍过。新增一条英文格式测试(orchestrator 测试文件里):

```ts
test('en locale 下注入格式用英文(投递时实时取词)', () => {
  setLocale('en');
  try {
    // 构造 deliverer 投一条消息,断言注入文本含 '[From ' 和 'sendmsg(to='
  } finally { setLocale('zh'); }
});
```
(具体构造方式抄同文件现有用例;先红——`[From` 尚不存在。)

- [ ] **Step 2:** 迁移:
- `index.ts`:HOUSE_RULES 整段 → `t().houseRules`(zh 原文逐字;en 翻译,工具名/参数名 register/sendmsg/idle/ask 保持原样);`你的身份:…` → `t().identityLine(name, role?)`;启动等待两行/`启动员工 X`/`✅ 办公室就绪`/`X 的窗口已关,自动下线`/防双开与竞态错误/端口回退告警 → 词典。**注意 bootstrap 在 launchInto 调用时实时取 t()**(规格 §6)。
- `orchestrator.ts`:注入格式 → `t().incomingMsg(from, body)`(zh:`【来自 ${from}】${body}\n(回复请调用 sendmsg(to="${from}", message="..."))`;en:`[From ${from}] ${body}\n(Reply via sendmsg(to="${from}", message="..."))`)。
- `bus/server.ts`:`对"…",老板选择:…` → `t().bossPicked(question, choice)`;MCP 工具 description(register/sendmsg/idle/ask)→ 词典。
- `templates.ts`:4 个预设团队 name/role → 词典(PRESETS 改为函数或 name/role 存 key 经 t() 取——看 setup 调用方,选最小改动且测试可过的方案);`bootstrapForRole` 派生句 → `t().roleBootstrap(role)`。**用户自存模板不动**。
- `guards.ts` 的 console.warn 两条 → 词典。
- [ ] **Step 3:** `npx tsc --noEmit && npm test` 全绿(默认 zh 一切照旧 + 新英文用例过)。
- [ ] **Step 4:** `git add -A && git commit -m "feat(i18n): 注入员工的全部文案入词典(协作规则/消息格式/工具描述/预设团队)"`

---

### Task 5: /admin/lang 端点 + 控制台 /lang 选择器

**Files:**
- Modify: `src/bus/server.ts`、`src/console/{commands,parse}.ts`、`src/console/app.tsx`
- Test: `tests/bus/lang.test.ts`(新)、`tests/console/parse.test.ts`(扩)

- [ ] **Step 1(先红):** `tests/bus/lang.test.ts`(起 bus 写法抄 tests/bus/admin.test.ts;settings 写盘要隔离——startBus 测试里语言切换会 saveSettings 到真实 ~/.falinks!**设计为 onLang 回调注入**,见 Step 2,测试只断言回调被调和返回值):

```ts
test('POST /admin/lang → 调用 onLang 并返回生效 locale', async () => {
  let got: string | null = null;
  const bus = await startBus({ router: mkRouter(), getSessionId: () => undefined,
    onLang: async (l) => { got = l; return 'en'; } }, 0);
  const r = await (await fetch(`http://127.0.0.1:${bus.port}/admin/lang`, {
    method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ locale: 'auto' }) })).json();
  expect(got).toBe('auto');
  expect(r).toEqual({ ok: true, locale: 'en' });
  await bus.close();
});
```

`tests/console/parse.test.ts` 追加:`/lang` → `{ kind: 'lang-start' }`;`/lang xx` → error(不收参数)。

- [ ] **Step 2:** 实现:
- `bus/server.ts`:BusDeps 加 `onLang?(locale: 'zh'|'en'|'auto'): Promise<Locale>`;路由 `POST /admin/lang` 校验 body.locale ∈ {zh,en,auto},调 onLang 返回 `{ok:true, locale}`。
- `index.ts` 接 onLang:`(l) => { const eff = l === 'auto' ? detectLocale(process.env) : l; setLocale(eff); saveSettings({ locale: l }); return eff; }`。
- `parse.ts`:`/lang`(无参)→ `{kind:'lang-start'}`;带参 → error(`t().langUsage`)。`commands.ts` COMMANDS 加 lang 条目。
- `app.tsx`:新增 langPicker state(复用向导/答题的 ↑↓+Enter 模式);选项 auto/zh/en(标签经 t(),当前值标记);确认 → `POST /admin/lang` → 用返回 locale `setLocale` 本进程 → `setStatus(t().langSwitched(...))`。**切换后整树重渲染**:setStatus 本身触发 render,t() 已换词典,够用。
- [ ] **Step 3:** `npx tsc --noEmit && npm test` 全绿。
- [ ] **Step 4:** `git add -A && git commit -m "feat(i18n): /admin/lang 端点 + 控制台 /lang 选择器(跨进程传播)"`

---

### Task 6: falinks lang CLI 子命令

**Files:**
- Modify: `src/cli.ts`;Create: `src/lang/run.tsx`(Ink 选择器,抄 setup/run.tsx 模式)

- [ ] **Step 1:** `cli.ts` switch 加 `case 'lang'`:TTY → 动态 import `./lang/run.js` 弹选择器;选定后 `saveSettings({locale})`、`setLocale(生效值)`、打印确认(经 t());再 `resolveBus(process.cwd())`,ok 则 POST /admin/lang 同步运行中的实例(失败静默)。非 TTY → 打印当前 locale 与提示。帮助文本加 lang。
- [ ] **Step 2:** `npx tsc --noEmit && npm run build && npm test` 全绿;手动 `npx tsx src/cli.ts lang` 选一遍确认交互(选择器渲染/Esc 取消)。
- [ ] **Step 3:** `git add -A && git commit -m "feat(i18n): falinks lang 子命令(选择器+同步运行中实例)"`

---

### Task 7: 文档 + 真机冒烟

- [ ] **Step 1:** CHANGELOG 0.2.7 段加 i18n 条目;README 特性区加双语说明(`/lang`、`falinks lang`、auto 检测)。
- [ ] **Step 2: 真机冒烟清单**(交给用户或 AppleScript):
  1. `LANG=en_US.UTF-8 falinks` → 全英文界面(含 slogan);`LANG=zh_CN.UTF-8` → 中文。
  2. 控制台 `/lang` 选 English → UI 立即全英;`@a 发消息` → 员工 pane 注入 `[From boss]`;`falinks lang` 选中文 → settings 落盘 + 运行实例同步。
  3. `/clear` 后重注入的 bootstrap 用当前语言。
  4. Ctrl+V 贴图英文界面下显示 `[Image 1]`,退格整删、发送展开路径均正常;切回中文后旧 `[Image 1]` 仍可展开。
- [ ] **Step 3:** `git add README.md CHANGELOG.md && git commit -m "docs: i18n 双语说明(0.2.7)"`

---

## Self-Review 结果

- **规格覆盖**:§1→T1,§2→T1,§3 表→T2/3/4,§4→T2,§5→T5/6,§6→T4,测试→各 task,限制/文档→T7。无缺口。
- **占位符**:T2-T4 的"通读迁移"按文件枚举了具体文案清单,正则给了目标形式并要求以现有实现为基准;无 TBD。
- **类型一致性**:`t()`/`setLocale`/`detectLocale`/`initLocale`/`loadSettings`/`saveSettings`/`onLang` 各任务间签名一致;`imageToken`/`incomingMsg`/`houseRules`/`identityLine`/`bossPicked`/`roleBootstrap` key 命名前后一致。
- **跨任务顺序**:T1 先建模块;T2-4 只增词典与替换;T5 依赖 T1 的 setLocale/detectLocale 与 T3 的 initLocale 接线;T6 依赖 T5 端点。顺序安全。
